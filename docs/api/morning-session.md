# API — Цены утренней сессии (`/api/morning-session`)

## Обзор

API для работы с ценами открытия утренней торговой сессии.

Предоставляет возможности для:
- **Загрузки цен** - сохранение цен открытия в базу данных
- **Предпросмотра цен** - получение данных без сохранения
- **Работы с датами** - за конкретную дату или сегодня
- **Фильтрации по типам** - акции, фьючерсы

**Базовый URL:** `http://localhost:8083/api/morning-session` (PROD) / `http://localhost:8087/api/morning-session` (TEST)

**Особенности:**
- **MorningSessionService** - сервис для обработки цен
- **Транзакционность** - все операции выполняются в транзакциях
- **Московское время** - используется Europe/Moscow для определения даты
- **Статистика** - детальная информация о результатах обработки

---

## POST /api/morning-session

Загрузка цен открытия утренней сессии за сегодня.

**Примеры использования:**
```bash
# Загрузка цен за сегодня
curl -X POST "http://localhost:8083/api/morning-session"
```

**Ответ (пример):**
```json
{
  "success": true,
  "message": "Успешно загружено 25 новых цен открытия из 30 найденных",
  "dateUsed": "2024-01-15",
  "statistics": {
    "totalRequested": 30,
    "found": 25,
    "notFound": 5,
    "saved": 20,
    "skippedExisting": 5
  },
  "timestamp": "2024-01-15T18:30:00"
}
```

---

## GET /api/morning-session

Предпросмотр цен открытия утренней сессии за сегодня (без сохранения в БД).

**Примеры использования:**
```bash
# Предпросмотр цен за сегодня
curl "http://localhost:8083/api/morning-session"
```

**Ответ (пример):**
```json
{
  "success": true,
  "message": "Найдено 25 цен открытия для предпросмотра",
  "dateUsed": "2024-01-15",
  "statistics": {
    "totalRequested": 30,
    "found": 25,
    "notFound": 5
  },
  "items": [
    {
      "figi": "BBG004730N88",
      "ticker": "SBER",
      "name": "Сбербанк",
      "openPrice": 250.50,
      "instrumentType": "SHARE"
    }
  ],
  "timestamp": "2024-01-15T18:30:00"
}
```

---

## POST /api/morning-session/{date}

Загрузка цен открытия утренней сессии за конкретную дату.

**Параметры запроса:**
- `date` (path) - дата в формате YYYY-MM-DD

**Примеры использования:**
```bash
# Загрузка цен за дату
curl -X POST "http://localhost:8083/api/morning-session/2024-01-15"
```

**Ответ (пример):**
```json
{
  "success": true,
  "message": "Успешно загружено 25 новых цен открытия из 30 найденных",
  "date": "2024-01-15",
  "statistics": {
    "totalRequested": 30,
    "found": 25,
    "notFound": 5,
    "saved": 20,
    "skippedExisting": 5
  },
  "timestamp": "2024-01-15T18:30:00"
}
```

---

## GET /api/morning-session/{date}

Поиск цен открытия утренней сессии за указанную дату (без сохранения в БД).

**Параметры запроса:**
- `date` (path) - дата в формате YYYY-MM-DD

**Примеры использования:**
```bash
# Поиск цен за дату
curl "http://localhost:8083/api/morning-session/2024-01-15"
```

**Ответ (пример):**
```json
{
  "success": true,
  "message": "Найдено 25 цен открытия для предпросмотра",
  "date": "2024-01-15",
  "statistics": {
    "totalRequested": 30,
    "found": 25,
    "notFound": 5
  },
  "items": [
    {
      "figi": "BBG004730N88",
      "ticker": "SBER",
      "name": "Сбербанк",
      "openPrice": 250.50,
      "instrumentType": "SHARE"
    }
  ],
  "timestamp": "2024-01-15T18:30:00"
}
```

---

## 🔧 Технические детали

### Архитектура
- **MorningSessionController** - основной контроллер для утренней сессии
- **MorningSessionService** - сервис для обработки цен
- **@Transactional** - все операции выполняются в транзакциях
- **SaveResponseDto** - DTO для ответов с результатами

### Источники данных
- **T-INVEST API** - получение данных о ценах
- **MorningSessionService** - обработка и сохранение данных
- **База данных** - сохранение результатов

### Временные зоны
- **Europe/Moscow** - используется для определения текущей даты
- **Локальное время** - все операции выполняются по московскому времени

### Статистика обработки
- **totalRequested** - общее количество запрошенных инструментов
- **found** - количество найденных цен
- **notFound** - количество инструментов без данных
- **saved** - количество сохраненных записей
- **skippedExisting** - количество пропущенных существующих записей

---

## 💡 Примеры использования

### Загрузка цен
```bash
# Загрузка за сегодня
curl -X POST "http://localhost:8083/api/morning-session"

# Загрузка за дату
curl -X POST "http://localhost:8083/api/morning-session/2024-01-15"
```

### Предпросмотр цен
```bash
# Предпросмотр за сегодня
curl "http://localhost:8083/api/morning-session"

# Поиск за дату
curl "http://localhost:8083/api/morning-session/2024-01-15"
```

### Мониторинг
```bash
# Проверка статистики
curl "http://localhost:8083/api/morning-session/2024-01-15" | jq '.statistics'
```

---

## ⚠️ Коды ответов

### Успешные ответы
- **200 OK** - данные получены или сохранены успешно

### Ошибки
- **500 Internal Server Error** - внутренняя ошибка сервера

### Примеры ошибок
```json
{
  "success": false,
  "message": "Ошибка загрузки цен утренней сессии: Connection timeout",
  "timestamp": "2024-01-15T18:30:00"
}
```

---

## 🔄 Жизненный цикл обработки

### Загрузка цен
1. **Запрос** - POST запрос с параметрами
2. **Определение даты** - сегодня или указанная дата
3. **Обработка сервисом** - получение данных из API
4. **Сохранение** - запись в базу данных
5. **Статистика** - подсчет результатов
6. **Ответ** - возврат статистики клиенту

### Предпросмотр цен
1. **Запрос** - GET запрос с параметрами
2. **Определение даты** - сегодня или указанная дата
3. **Обработка сервисом** - получение данных из API
4. **Форматирование** - преобразование в DTO
5. **Ответ** - возврат данных клиенту

### Обработка ошибок
1. **Валидация** - проверка параметров запроса
2. **Логирование** - детальная информация об ошибках
3. **Уведомление** - возврат ошибки клиенту
4. **Мониторинг** - отслеживание частоты ошибок
