# API — Системные эндпоинты (`/api/system`)

## Обзор

API для системного мониторинга и диагностики предоставляет следующие возможности:

- **Здоровье системы** - проверка состояния основных компонентов
- **Диагностика** - детальная информация о системе и ресурсах
- **Статистика** - метрики производительности и использования ресурсов
- **Информация о системе** - версии, конфигурация, окружение
- **Внешние сервисы** - проверка доступности внешних зависимостей
- **Материализованные представления** - проверка и управление агрегацией данных

**Базовый URL:** `http://localhost:8083/api/system` (PROD) / `http://localhost:8087/api/system` (TEST)

---

## GET /api/system/health

Проверка здоровья системы и основных компонентов.

Возвращает общий статус системы и состояние ключевых компонентов (БД, материализованные представления, планировщики, API).

**Примеры использования:**
```bash
# Проверка здоровья системы
curl "http://localhost:8083/api/system/health"
```

**Ответ (пример):**
```json
{
  "status": "healthy",
  "timestamp": "2024-01-15T10:30:00",
  "components": {
    "database": "healthy",
    "materialized_views": "warning",
    "schedulers": "healthy",
    "api": "healthy"
  },
  "uptime": 1737043200000
}
```

**Возможные статусы:**
- `healthy` - компонент работает нормально
- `warning` - компонент работает с предупреждениями
- `unhealthy` - компонент не работает

## GET /api/system/diagnostics

Детальная диагностика системы.

Возвращает подробную информацию о состоянии системы, включая версию Java, ОС, память, расписания и состояние материализованных представлений.

**Примеры использования:**
```bash
# Детальная диагностика
curl "http://localhost:8083/api/system/diagnostics"
```

**Ответ (пример):**
```json
{
  "success": true,
  "data": {
    "materialized_views": {
      "exists": false,
      "status": "missing"
    },
    "system": {
      "java_version": "21.0.8",
      "os_name": "Windows 10",
      "os_version": "10.0",
      "available_processors": 8,
      "max_memory": 4294967296,
      "total_memory": 1073741824,
      "free_memory": 536870912
    },
    "schedules": {
      "daily_refresh": "0 * * * * * (каждую минуту)",
      "full_refresh": "0 20 2 * * * (каждый день в 2:20)",
      "timezone": "Europe/Moscow"
    }
  },
  "timestamp": "2024-01-15T10:30:00"
}
```

**Поля ответа:**
- `materialized_views` - состояние материализованных представлений
- `system` - информация о Java и ОС
- `schedules` - расписания обновлений

## GET /api/system/volume-aggregation/check

Проверка существования материализованных представлений.

Проверяет наличие и доступность материализованных представлений для агрегации объемов торгов.

**Примеры использования:**
```bash
# Проверка материализованных представлений
curl "http://localhost:8083/api/system/volume-aggregation/check"
```

**Ответ (пример):**
```json
{
  "success": true,
  "exists": false,
  "message": "Материализованные представления не найдены",
  "timestamp": "2024-01-15T10:30:00"
}
```

**Поля ответа:**
- `success` - статус операции
- `exists` - существуют ли представления
- `message` - описание состояния
- `timestamp` - время проверки

## GET /api/system/volume-aggregation/schedule-info

Информация о расписаниях обновлений материализованных представлений.

Возвращает детальную информацию о расписаниях обновления агрегированных данных.

**Примеры использования:**
```bash
# Информация о расписаниях
curl "http://localhost:8083/api/system/volume-aggregation/schedule-info"
```

**Ответ (пример):**
```json
{
  "success": true,
  "data": {
    "daily_refresh": "0 * * * * * (каждую минуту)",
    "full_refresh": "0 20 2 * * * (каждый день в 2:20)",
    "timezone": "Europe/Moscow",
    "description": "Дневное представление обновляется каждую минуту, общее - в 2:20"
  },
  "timestamp": "2024-01-15T10:30:00"
}
```

**Поля ответа:**
- `daily_refresh` - расписание обновления дневного представления
- `full_refresh` - расписание полного обновления
- `timezone` - временная зона расписаний
- `description` - описание расписаний

## GET /api/system/stats

Статистика системы и ресурсов.

Возвращает сводную статистику по ресурсам JVM, процессору и времени работы приложения.

**Примеры использования:**
```bash
# Статистика системы
curl "http://localhost:8083/api/system/stats"
```

**Ответ (пример):**
```json
{
  "success": true,
  "data": {
    "memory": {
      "max_memory_mb": 4096,
      "total_memory_mb": 1024,
      "free_memory_mb": 512,
      "used_memory_mb": 512
    },
    "processor": {
      "available_processors": 8
    },
    "uptime": {
      "start_time": 1737043200000,
      "uptime_ms": 1737046800000
    },
    "volume_aggregation": {
      "status": "not_implemented"
    }
  },
  "timestamp": "2024-01-15T10:30:00"
}
```

**Поля ответа:**
- `memory` - статистика использования памяти JVM
- `processor` - информация о процессоре
- `uptime` - время работы приложения
- `volume_aggregation` - статус агрегации данных

## GET /api/system/info

Информация о системе и приложении.

Возвращает детальную информацию о версии приложения, JVM, ОС и пользователе.

**Примеры использования:**
```bash
# Информация о системе
curl "http://localhost:8083/api/system/info"
```

**Ответ (пример):**
```json
{
  "success": true,
  "data": {
    "java": {
      "version": "21.0.8",
      "vendor": "Oracle",
      "home": "/usr/lib/jvm/java-21"
    },
    "os": {
      "name": "Windows 10",
      "version": "10.0",
      "arch": "amd64"
    },
    "user": {
      "name": "admin",
      "home": "/home/admin",
      "dir": "/app"
    },
    "application": {
      "name": "Investment Data Loader Service",
      "version": "1.0.0",
      "description": "Сервис загрузки инвестиционных данных"
    }
  },
  "timestamp": "2024-01-15T10:30:00"
}
```

**Поля ответа:**
- `java` - информация о Java Runtime Environment
- `os` - информация об операционной системе
- `user` - информация о пользователе
- `application` - информация о приложении

## GET /api/system/external-services

Проверка доступности внешних сервисов.

Проверяет состояние внешних зависимостей приложения (Tinkoff API, база данных).

**Примеры использования:**
```bash
# Проверка внешних сервисов
curl "http://localhost:8083/api/system/external-services"
```

**Ответ (пример):**
```json
{
  "success": true,
  "data": {
    "tinkoff_api": {
      "status": "unknown",
      "message": "Проверка не реализована"
    },
    "database": {
      "status": "healthy",
      "message": "Подключение к базе данных работает"
    }
  },
  "timestamp": "2024-01-15T10:30:00"
}
```

**Поля ответа:**
- `tinkoff_api` - статус Tinkoff Invest API
- `database` - статус подключения к базе данных

**Возможные статусы:**
- `healthy` - сервис доступен
- `unhealthy` - сервис недоступен
- `unknown` - статус не определен

---

## 🔧 Технические детали

### Статусы здоровья
- **healthy** - компонент работает нормально
- **warning** - компонент работает с предупреждениями
- **unhealthy** - компонент не работает

### Коды ответов
- **200 OK** - успешный запрос
- **500 Internal Server Error** - внутренняя ошибка сервера
- **503 Service Unavailable** - система недоступна

### Особенности реализации
- **Материализованные представления** - проверка пока не реализована (TODO)
- **Внешние сервисы** - базовая проверка Tinkoff API
- **Статистика агрегации** - детальная статистика не реализована
- **Время работы** - упрощенная реализация

### Мониторинг
- **Health Check** - для load balancer и мониторинга
- **Диагностика** - для отладки и анализа проблем
- **Статистика** - для мониторинга производительности
- **Внешние сервисы** - для проверки зависимостей
