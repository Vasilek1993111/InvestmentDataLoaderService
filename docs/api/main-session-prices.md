# API — Цены основной сессии (`/api/main-session-prices`)

## Обзор

API для работы с ценами основной торговой сессии и ценами закрытия.

Предоставляет возможности для:
- **Загрузки цен** - сохранение цен в базу данных
- **Получения цен** - получение данных из T-INVEST API
- **Фильтрации по типам** - акции, фьючерсы, индикативы
- **Работы с датами** - за конкретную дату или сегодня

**Базовый URL:** `http://localhost:8083/api/main-session-prices` (PROD) / `http://localhost:8087/api/main-session-prices` (TEST)

**Особенности:**
- **Транзакционность** - все операции выполняются в транзакциях
- **Валидация дат** - проверка рабочих дней
- **Кэширование** - использование CachedInstrumentService
- **Фильтрация данных** - исключение невалидных цен

---

## POST /api/main-session-prices/

Загрузка цен закрытия основной сессии за сегодня.

Работает с акциями и фьючерсами одновременно, использует кэш и T-INVEST API.

**Примеры использования:**
```bash
# Загрузка всех цен закрытия за сегодня
curl -X POST "http://localhost:8083/api/main-session-prices/"
```

**Ответ (пример):**
```json
{
  "success": true,
  "message": "Успешно загружено 150 новых цен закрытия основной сессии из 200 найденных.",
  "date": "2024-01-15",
  "totalRequested": 200,
  "newItemsSaved": 150,
  "existingItemsSkipped": 30,
  "invalidItemsFiltered": 15,
  "missingFromApi": 5,
  "savedItems": [
    {
      "figi": "BBG004730N88",
      "ticker": "SBER",
      "name": "Сбербанк",
      "tradingDate": "2024-01-15",
      "closePrice": 250.75,
      "currency": "RUB"
    }
  ],
  "timestamp": "2024-01-15T18:30:00"
}
```


---

## GET /api/main-session-prices/shares

Получение цен закрытия для всех акций из T-INVEST API.

**Примеры использования:**
```bash
# Получение цен закрытия акций
curl "http://localhost:8083/api/main-session-prices/shares"
```

**Ответ (пример):**
```json
{
  "success": true,
  "message": "Цены закрытия для акций получены успешно",
  "data": [
    {
      "figi": "BBG004730N88",
      "ticker": "SBER",
      "tradingDate": "2024-01-15",
      "closePrice": 250.75
    }
  ],
  "count": 1,
  "timestamp": "2024-01-15T18:30:00"
}
```

---

## POST /api/main-session-prices/shares

Загрузка цен закрытия для всех акций из T-INVEST API в БД.

**Примеры использования:**
```bash
# Загрузка цен закрытия акций
curl -X POST "http://localhost:8083/api/main-session-prices/shares"
```

---

## GET /api/main-session-prices/futures

Получение цен закрытия для всех фьючерсов из T-INVEST API.

**Примеры использования:**
```bash
# Получение цен закрытия фьючерсов
curl "http://localhost:8083/api/main-session-prices/futures"
```

---

## POST /api/main-session-prices/futures

Загрузка цен закрытия для всех фьючерсов из T-INVEST API в БД.

**Примеры использования:**
```bash
# Загрузка цен закрытия фьючерсов
curl -X POST "http://localhost:8083/api/main-session-prices/futures"
```

---

## GET /api/main-session-prices/{figi}

Получение цены закрытия по конкретному инструменту из T-INVEST API.

**Параметры запроса:**
- `figi` (path) - идентификатор инструмента

**Примеры использования:**
```bash
# Получение цены закрытия по инструменту
curl "http://localhost:8083/api/main-session-prices/BBG004730N88"
```

---

## POST /api/main-session-prices/{figi}

Загрузка цены закрытия по конкретному инструменту из T-INVEST API в БД.

**Параметры запроса:**
- `figi` (path) - идентификатор инструмента

**Примеры использования:**
```bash
# Загрузка цены закрытия по инструменту
curl -X POST "http://localhost:8083/api/main-session-prices/BBG004730N88"
```

---

## POST /api/main-session-prices/{date}

Загрузка цен основной сессии за конкретную дату.

Работает только с рабочими днями, использует данные из `minute_candles`, обрабатывает только акции и фьючерсы.

**Параметры запроса:**
- `date` (path) - дата в формате YYYY-MM-DD

**Примеры использования:**
```bash
# Загрузка цен основной сессии за дату
curl -X POST "http://localhost:8083/api/main-session-prices/2024-01-15"
```

**Ответ (пример):**
```json
{
  "success": true,
  "message": "Успешно загружено 200 новых цен основной сессии из 250 найденных.",
  "date": "2024-01-15",
  "totalRequested": 250,
  "newItemsSaved": 200,
  "existingItemsSkipped": 30,
  "invalidItemsFiltered": 15,
  "missingFromApi": 5,
  "savedItems": [
    {
      "figi": "BBG004730N88",
      "ticker": "SBER",
      "name": "Сбербанк",
      "tradingDate": "2024-01-15",
      "closePrice": 250.75,
      "currency": "RUB"
    }
  ]
}
```

---

## 🔧 Технические детали

### Архитектура
- **MainSessionPricesController** - основной контроллер для цен основной сессии
- **MainSessionPriceService** - сервис для обработки цен
- **InstrumentService** - сервис для работы с инструментами
- **@Transactional** - все операции выполняются в транзакциях

### Валидация дат
- **Проверка рабочих дней** - все методы с датами проверяют рабочие дни
- **Выходные дни** - суббота и воскресенье возвращают ошибку
- **Московское время** - используется Europe/Moscow для определения дня недели

### Источники данных
- **POST методы** - используют данные из таблицы `minute_candles`
- **GET методы** - получают данные из T-INVEST API
- **Кэширование** - использование CachedInstrumentService
- **Сохранение** - все данные сохраняются в таблицу `close_prices`

### Фильтрация данных
- **Исключение невалидных** - цены с датой 1970-01-01
- **Валидация цен** - проверка цен больше 0
- **Проверка существования** - проверка записей перед сохранением
- **Фильтрация по валюте** - только инструменты в рублях

### Типы инструментов
- **SHARES** - акции
- **FUTURES** - фьючерсы
- **INDICATIVES** - индикативные инструменты

---

## 💡 Примеры использования

### Загрузка цен
```bash
# Загрузка всех цен закрытия за сегодня
curl -X POST "http://localhost:8083/api/main-session-prices/"

# Загрузка цен основной сессии за дату
curl -X POST "http://localhost:8083/api/main-session-prices/2024-01-15"

# Загрузка цен акций
curl -X POST "http://localhost:8083/api/main-session-prices/shares"

# Загрузка цен фьючерсов
curl -X POST "http://localhost:8083/api/main-session-prices/futures"

# Загрузка цены по инструменту
curl -X POST "http://localhost:8083/api/main-session-prices/BBG004730N88"
```

### Получение цен
```bash
# Получение цен акций
curl "http://localhost:8083/api/main-session-prices/shares"

# Получение цен фьючерсов
curl "http://localhost:8083/api/main-session-prices/futures"

# Получение цены по инструменту
curl "http://localhost:8083/api/main-session-prices/BBG004730N88"
```

---

## ⚠️ Коды ответов

### Успешные ответы
- **200 OK** - данные получены или сохранены успешно

### Ошибки
- **400 Bad Request** - некорректные параметры запроса
- **500 Internal Server Error** - внутренняя ошибка сервера

### Примеры ошибок
```json
{
  "success": false,
  "message": "В выходные дни (суббота и воскресенье) основная сессия не проводится. Дата: 2024-01-14",
  "date": "2024-01-14",
  "totalRequested": 0,
  "newItemsSaved": 0,
  "existingItemsSkipped": 0,
  "invalidItemsFiltered": 0,
  "missingFromApi": 0,
  "savedItems": []
}
```

---

## 🔄 Жизненный цикл обработки

### Загрузка цен
1. **Запрос** - POST запрос с параметрами
2. **Валидация** - проверка даты и параметров
3. **Получение инструментов** - из кэша или репозиториев
4. **Обработка** - получение данных из API или БД
5. **Фильтрация** - валидация и очистка данных
6. **Сохранение** - запись в базу данных
7. **Ответ** - возврат статистики клиенту

### Получение цен
1. **Запрос** - GET запрос с параметрами
2. **Обработка** - получение данных из API
3. **Фильтрация** - валидация данных
4. **Форматирование** - преобразование в DTO
5. **Ответ** - возврат данных клиенту

### Обработка ошибок
1. **Валидация** - проверка параметров запроса
2. **Логирование** - детальная информация об ошибках
3. **Уведомление** - возврат ошибки клиенту
4. **Мониторинг** - отслеживание частоты ошибок
